# CLI 구현 검증

- CLI 패키지 0.2.0 / 판정 규칙 0.1.0.
- 로컬 Windows, Node.js 22.17.0, npm 10.9.2.
- npm test: 명령 인터페이스 18개 + 기존 판정 엔진 23개, 총 41개 통과 / 실패 0개.
- npm run smoke:install: tarball 생성, 격리 폴더에 오프라인 설치, 설치된 명령 실행 등 5개 검사 통과.
- 시스템 전역 설치를 변경하지 않았습니다. 테스트 설치는 생성한 임시 폴더 안에서 이루어지고 정리되었습니다.
- 배포 패키지에는 HTML·CSS·웹 서버가 없습니다.
- 원격 GitHub Actions는 실행하지 않았습니다. 게시 연결에 `workflow` 권한이 없어 Windows/Linux/macOS 및 Node 22/24 설정은 [수동 등록용 예제](docs/ci-workflow.yml)로 포함합니다.

## 재검증

다운로드한 폴더에서 다음 명령을 실행합니다.

~~~text
npm test
npm run smoke:install
~~~

표시된 검사는 소프트웨어의 입력 처리·판정 분기·출력·설치 동작에 관한 것입니다. 프레임워크의 현장 효과나 법적 판단 정확성을 입증하는 연구 결과가 아닙니다. 원고 사례 1개와 합성 사례 9개는 구분되어 있습니다.

manifest.json은 전달 파일의 대응을 확인하는 SHA-256 목록이며 외부 사전등록이나 신뢰할 수 있는 타임스탬프가 아닙니다.
